# uv 실행 바이너리(없으면 which 로 자동 탐색; macOS 기본 경로도 시도)
MCP_YF_CMD=uv

# yahoo-finance-mcp 디렉토리 (절대경로 권장. 비우면 코드가 자동 추정 시도)
# 예: /Users/you/dev/a2a-mcp-multiagent-poc/mcp-server/yahoo-finance-mcp
MCP_YF_DIR= 

# 초기화/호출 타임아웃(초)
MCP_CALL_TIMEOUT=90

# 로그 레벨
LOG_LEVEL=INFO
